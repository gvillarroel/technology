# JSON loader operations

This approach is useful when page metadata is already structured in data files.

- [Back to overview](./overview)

It is simpler than a remote loader and still supports dual-format generation.
